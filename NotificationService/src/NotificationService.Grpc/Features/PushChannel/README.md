# PushChannel

Prazan skelet — kasnije, van obima ove faze (v. artifact "Notification gRPC Flow").

Isti obrazac kao `Features/EmailChannel/` i `Features/SmsChannel/`, kad dođe
na red (FCM/APNs). `NotificationQueueConsumer` već ima
`case NotificationChannel.Push` pripremljen.
